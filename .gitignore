/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.env
.env*.local
.DS_Store

# zamówienia klientów (trzymane na serwerze, nie w repo)
/data/zamowienia.json

# baza produktów — żywa kopia trzymana na serwerze (edycje z panelu).
# W repo jest tylko wzorzec products.seed.json; ten plik NIE jest nadpisywany przy deployu.
/data/products.json

# wgrane zdjęcia produktów (trzymane na serwerze, nie w repo)
/public/uploads

# zdjęcia produktów wgrywane przez panel (nie do repo) — luźne pliki w katalogu głównym
/*.jpg
/*.jpeg
/*.png
/*.webp
